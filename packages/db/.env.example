# PostgreSQL 连接串
# 默认指向 docker/docker-compose.yml 起的本地 pg
# 用户名 / 密码 / 端口与 compose 中保持一致
#
# 可复制仓库根目录的 .env 中的 DATABASE_URL，或：
#   copy ..\..\.env.example .env   （在 packages\db 下执行时按实际路径调整）
DATABASE_URL=postgresql://fitness:fitness@127.0.0.1:5432/fitness?schema=public
