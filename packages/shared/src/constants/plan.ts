/** 默认 mesocycle 周数（PRD §5.1） */
export const DEFAULT_MESOCYCLE_WEEKS = 4;

/** 每周训练日数量上下界（PRD §5.1：每个微周期 3–6 个训练日） */
export const TRAINING_DAYS_PER_WEEK_MIN = 3;
export const TRAINING_DAYS_PER_WEEK_MAX = 6;
